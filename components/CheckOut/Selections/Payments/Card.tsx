import React from 'react';
import { StyleSheet, View } from 'react-native';

const Card = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Card;
