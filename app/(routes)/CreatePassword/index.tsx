import { View, Text } from 'react-native'
import React from 'react'
import CreatePassword from '@/components/Auth/Passwords/CreatePassword'

export default function CreatePasswords() {
  return (
   <CreatePassword/>
  )
}