import { View, Text } from 'react-native'
import React from 'react'
import EmailSetup from '@/components/Auth/Signup/emailRegister/emailSetup'

export default function emailRegister() {
  return (
    <EmailSetup/>
  )
}