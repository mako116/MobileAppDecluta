import HeaderMulti from '@/components/SearchPage/Multiple-products/HeaderMulti';
import SearchResultsScreen from '@/components/SearchPage/Multiple-products/MultileProducts';
import MultipleProd from '@/components/SearchPage/Multiple-products/MultileProducts';
import React from 'react';
import { StyleSheet, View } from 'react-native';

const Multi = () => {
    return (
        <View>
            <HeaderMulti/>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Multi;
