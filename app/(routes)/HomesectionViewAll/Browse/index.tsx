import HeaderBrowse from '@/screens/HomeSectionFinds/BrowseCategory/HeaderBrowse';
import React from 'react';
import { StyleSheet, View } from 'react-native';

const BrowseView = () => {
    return (
        <View>
            <HeaderBrowse/>
        </View>
    );
}

const styles = StyleSheet.create({})

export default BrowseView;
